Secure Chat Protocol:

Make sure to do "pip install argon2-cffi cryptography" before running this.

Instructions:
To start, run "python server.py" in terminal
Then, in a split terminal or separate one, run "python client.py"

